# Slideshow Video Creator Pro - Code Explanation

Αυτή η έκδοση αποτελεί την επαγγελματική αναβάθμιση της εφαρμογής, βελτιστοποιημένη για ταχύτητα και ευχρηστία.

## Κύριες Αναβαθμίσεις

### 1. Ενοποιημένο Σύστημα Ρυθμίσεων Ήχου
Όλες οι λειτουργίες ήχου συγκεντρώθηκαν στο παράθυρο **Settings > Audio & Music Settings**:
- **Ένταση (dB)**: Ρύθμιση έντασης της μουσικής.
- **Mute Video**: Επιλογή για πλήρη σίγαση του αρχικού ήχου των βίντεο.
- **Music Folder**: Επιλογή οποιουδήποτε φακέλου μουσικής στον υπολογιστή.
- **Refresh/Reset**: Κουμπιά για ενημέρωση της λίστας ή μηδενισμό των χρησιμοποιημένων τραγουδιών.
- **Crossfade**: Ρυθμιζόμενη ομαλή μετάβαση (σε δευτερόλεπτα) ανάμεσα στα τραγούδια.
- **Auto Trim Silence**: Αυτόματη αφαίρεση της ησυχίας από την αρχή και το τέλος κάθε τραγουδιού.

### 2. Thumbnail View (Προβολή Εικονιδίων)
Αντικαταστάθηκε η απλή λίστα με ένα δυναμικό σύστημα εικονιδίων:
- **Προεπισκόπηση**: Κάθε βίντεο ή εικόνα εμφανίζεται ως μικρογραφία.
- **Διαχείριση**: Με **δεξί κλικ** πάνω σε οποιοδήποτε εικονίδιο, ο χρήστης μπορεί να το διαγράψει ή να το μετακινήσει (Move Left/Right) για να αλλάξει τη σειρά.
- **Drag & Drop**: Υποστήριξη για μεταφορά αρχείων απευθείας στην περιοχή των εικονιδίων.

### 3. Μηχανή FFmpeg Turbo
Η διαδικασία του export άλλαξε ριζικά:
- **FFmpeg-First**: Η εφαρμογή δεν βασίζεται πλέον μόνο στο MoviePy, αλλά καλεί απευθείας το FFmpeg μέσω subprocess.
- **Speed**: Η δημιουργία των clips και η ένωσή τους γίνεται σε επίπεδο stream, καθιστώντας το export έως και 5 φορές ταχύτερο.
- **Audio Processing**: Ο ήχος επεξεργάζεται ξεχωριστά με φίλτρα `acrossfade` και `silenceremove` για επαγγελματικό αποτέλεσμα.

### 4. Markdown Bold & Wrapping
- **Markdown**: Το κείμενο υποστηρίζει πλέον πλήρως τη σύνταξη `**bold**`.
- **Smart Wrapping**: Το κείμενο στοιχίζεται αυτόματα στο κέντρο, υπολογίζοντας το πλάτος κάθε λέξης και σεβόμενο τις αλλαγές γραμμής του χρήστη.
- **Black Box**: Το λευκό πλαίσιο πίσω από το κείμενο προσαρμόζεται αυτόματα στο μέγεθος των γραμμών για μέγιστη αναγνωσιμότητα.

## Τεχνική Δομή
- **Threading**: Το Preview και το Export εκτελούνται σε background threads για να παραμένει το GUI ενεργό.
- **Temp Management**: Χρησιμοποιείται ο φάκελος `temp/` για την αποθήκευση ενδιάμεσων PNG και MP4 αρχείων, τα οποία συνδυάζονται στο τελικό αποτέλεσμα.
- **Persistence**: Όλες οι ρυθμίσεις και το ιστορικό μουσικής αποθηκεύονται σε JSON αρχεία για να διατηρούνται μετά το κλείσιμο της εφαρμογής.
